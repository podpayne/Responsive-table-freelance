Client wanted his table to be responsive, 
I suggested that instead of using horizontal scroll for table responsivness,
we could create a sort of card for each row in the table, with columns on the left side of the card,
and their data on the right.
